- pipeline: "Build and make it public"
  trigger_mode: "ON_EVERY_PUSH"
  ref_name: "master"
  actions:
    - action: "Install and build"
      type: "BUILD"
      execute_commands:
        - "npm install"
        - "npm run build"

