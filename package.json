{
  "name": "company-site",
  "version": "2.0.0",
  "description": "Skillbill website",
  "main": "build.js",
  "private": true,
  "scripts": {
    "build-watch": "node build.js --watch",
    "build": "node build.js",
    "http-server": "http-server dist -p 8080 -c-1",
    "dev": "npm-run-all --parallel build-watch http-server"
  },
  "devDependencies": {
    "http-server": "^0.12.1",
    "npm-run-all": "^4.1.5"
  }
}
