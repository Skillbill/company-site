# config.yml

backend:
  name: github
  repo: Skillbill/company-site
  auth_type: implicit
  app_id: 6065479b0e2d7a4207f4
media_folder: 'images'
collections:
  - name: 'blog'
    label: 'Blog'
    folder: '_posts/'
    create: true # Allow users to create new documents in this collection
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields:
      - { name: Ciccio }
